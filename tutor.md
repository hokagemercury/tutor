# 📚 Полный туториал по запуску VPN бота с нуля

## 📋 Содержание

1. [Требования](#требования)
2. [Установка зависимостей](#установка-зависимостей)
3. [Настройка переменных окружения](#настройка-переменных-окружения)
4. [Запуск бота](#запуск-бота)
5. [Настройка API на VPS для VPN серверов](#настройка-api-на-vps-для-vpn-серверов)
6. [Настройка API для LTE Bypass](#настройка-api-для-lte-bypass)
7. [Настройка админ-панели](#настройка-админ-панели)
8. [Добавление серверов через админ-панель](#добавление-серверов-через-админ-панель)
9. [Проверка работы](#проверка-работы)
10. [Запуск через PM2 (продакшен)](#запуск-через-pm2-продакшен)

---

## 🔧 Требования

### На сервере с ботом:
- **Python 3.8+**
- **Node.js 18+** (для админ-панели)
- **SQLite3** (обычно уже установлен)
- **Git** (для клонирования репозитория)

### На VPN серверах (VPS):
- **Ubuntu/Debian** (рекомендуется)
- **Xray** (устанавливается автоматически)
- **Python 3** (для API)
- **systemd** (для автозапуска)

---

## 📦 Установка зависимостей

### 1. Клонирование репозитория

```bash
git clone <your-repo-url> vpn_end
cd vpn_end
```

### 2. Установка Python зависимостей

```bash
# Обновляем pip
python3 -m pip install --upgrade pip --user

# Устанавливаем зависимости
python3 -m pip install -r requirements.txt --user

# Проверка установки
python3 -c "import aiogram; print('✅ aiogram установлен')"
```

**Если возникает ошибка `externally-managed-environment`:**
```bash
# Используйте флаг --user (рекомендуется)
python3 -m pip install -r requirements.txt --user

# Или используйте виртуальное окружение
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Установка Node.js зависимостей

```bash
# Для админ-панели API
cd admin-api
npm install
cd ..

# Для веб-интерфейса админ-панели
cd admin
npm install
cd ..
```

---

## ⚙️ Настройка переменных окружения

### 1. Создайте файл `.env` в корне проекта

```bash
nano .env
```

### 2. Добавьте следующие переменные:

```env
# Telegram Bot Token (получите у @BotFather)
BOT_TOKEN=your_telegram_bot_token_here

# Путь к базе данных (по умолчанию)
DB_PATH=data/vpn_bot.db

# CryptoBot API (опционально, для криптоплатежей)
CRYPTOBOT_TOKEN=your_cryptobot_token_here

# Lava API (опционально, для СБП платежей)
LAVA_SECRET_KEY=your_lava_secret_key
LAVA_SHOP_ID=your_lava_shop_id

# Админ-панель
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password_here
PORT=8080
CACHE_SERVER_URL=http://localhost:8888
```

### 3. Сохраните файл (Ctrl+O, Enter, Ctrl+X)

---

## 🚀 Запуск бота

### Простой запуск (для тестирования)

```bash
python3 main.py
```

Бот должен запуститься и вывести сообщение:
```
@your_bot_username (bot_id) is now running.
```

### Проверка работы

Откройте Telegram и найдите вашего бота, отправьте команду `/start`.

---

## 🔌 Настройка API на VPS для VPN серверов

Для того, чтобы бот мог автоматически добавлять/удалять UUID на VPN серверах, нужно настроить API на каждом сервере.

### 1. Подключитесь к VPN серверу

```bash
ssh root@your-vps-ip
```

### 2. Установите Xray (если ещё не установлен)

```bash
bash -c "$(curl -L https://github.com/XTLS/Xray-install/raw/main/install-release.sh)" @ install
```

### 3. Скопируйте скрипт API на сервер

**С вашего компьютера:**
```bash
scp scripts/vpn_server_api.py root@your-vps-ip:/root/
```

**Или создайте файл на сервере:**
```bash
nano /root/vpn_server_api.py
# Вставьте содержимое файла scripts/vpn_server_api.py
```

### 4. Сделайте файл исполняемым

```bash
chmod +x /root/vpn_server_api.py
```

### 5. Создайте systemd сервис

```bash
nano /etc/systemd/system/vpn-api.service
```

Вставьте следующее содержимое:

```ini
[Unit]
Description=VPN Server API
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root
ExecStart=/usr/bin/python3 /root/vpn_server_api.py
Restart=always
RestartSec=10
Environment="VPN_API_TOKEN=your_secure_token_here_change_me"
Environment="VPN_API_PORT=8889"

[Install]
WantedBy=multi-user.target
```

**⚠️ ВАЖНО:** Замените `your_secure_token_here_change_me` на случайный безопасный токен!

```bash
# Генерируем случайный токен
openssl rand -hex 32
```

Скопируйте сгенерированный токен и вставьте в файл вместо `your_secure_token_here_change_me`.

### 6. Запустите сервис

```bash
systemctl daemon-reload
systemctl enable vpn-api
systemctl start vpn-api
systemctl status vpn-api
```

### 7. Проверьте работу API

```bash
# Проверка статуса
systemctl status vpn-api

# Проверка порта
netstat -tlnp | grep 8889

# Тест API
curl -X POST http://localhost:8889/list-clients \
  -H "Authorization: Bearer your_secure_token_here"
```

### 8. Откройте порт в firewall

```bash
# Для ufw
ufw allow 8889/tcp

# Для iptables
iptables -A INPUT -p tcp --dport 8889 -j ACCEPT
```

### 9. Запишите данные для админ-панели

Вам понадобятся:
- **API URL:** `http://your-vps-ip:8889`
- **API Token:** тот токен, который вы сгенерировали

---

## 📡 Настройка API для LTE Bypass

Для услуги "Обход белых списков" нужен отдельный API сервер на hop-сервере.

### 1. Подключитесь к hop-серверу (сервер для обхода белых списков)

```bash
ssh root@your-hop-server-ip
```

### 2. Скопируйте скрипт API на сервер

**С вашего компьютера:**
```bash
scp scripts/lte_bypass_server_api.py root@your-hop-server-ip:/root/
```

### 3. Сделайте файл исполняемым

```bash
chmod +x /root/lte_bypass_server_api.py
```

### 4. Создайте systemd сервис

```bash
nano /etc/systemd/system/lte-bypass-api.service
```

Вставьте следующее содержимое:

```ini
[Unit]
Description=LTE Bypass Server API
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root
ExecStart=/usr/bin/python3 /root/lte_bypass_server_api.py
Restart=always
RestartSec=10
Environment="LTE_BYPASS_API_TOKEN=your_secure_token_here_change_me"
Environment="LTE_BYPASS_API_PORT=8890"

[Install]
WantedBy=multi-user.target
```

**⚠️ ВАЖНО:** Замените `your_secure_token_here_change_me` на случайный безопасный токен!

```bash
# Генерируем случайный токен
openssl rand -hex 32
```

### 5. Запустите сервис

```bash
systemctl daemon-reload
systemctl enable lte-bypass-api
systemctl start lte-bypass-api
systemctl status lte-bypass-api
```

### 6. Проверьте работу API

```bash
# Проверка статуса
systemctl status lte-bypass-api

# Проверка порта
netstat -tlnp | grep 8890

# Тест API
curl -X POST http://localhost:8890/list-clients \
  -H "Authorization: Bearer your_secure_token_here"
```

### 7. Откройте порт в firewall

```bash
# Для ufw
ufw allow 8890/tcp

# Для iptables
iptables -A INPUT -p tcp --dport 8890 -j ACCEPT
```

### 8. Запишите данные для админ-панели

Вам понадобятся:
- **API URL:** `http://your-hop-server-ip:8890`
- **API Token:** тот токен, который вы сгенерировали

---

## 🎨 Настройка админ-панели

### 1. Запустите API сервер админ-панели

```bash
cd admin-api
npm start
```

Или через PM2 (рекомендуется для продакшена):
```bash
pm2 start ecosystem.config.js --only admin-api
```

### 2. Запустите веб-интерфейс админ-панели

**Для разработки:**
```bash
cd admin
npm run dev
```

**Для продакшена:**
```bash
cd admin
npm run build
# Затем используйте serve или nginx (см. ниже)
```

### 3. Откройте админ-панель в браузере

```
http://your-server-ip:5173
```

**Дефолтные учётные данные:**
- Username: `admin`
- Password: `admin` (или тот, который вы указали в `.env`)

### 4. Проверка работы

Если всё настроено правильно, вы увидите:
- Дашборд с статистикой
- Список пользователей
- Управление серверами
- Логи платежей

---

## 🖥️ Добавление серверов через админ-панель

### 1. Откройте админ-панель

```
http://your-server-ip:5173
```

### 2. Перейдите в раздел "Серверы"

Нажмите на вкладку "Серверы" в боковом меню.

### 3. Нажмите "Добавить сервер"

Заполните форму:

#### Для обычного VPN сервера:

- **Название:** `Frankfurt-1` (или любое другое)
- **Локация:** `Германия` (или другая страна)
- **Host:** IP адрес сервера (например: `193.58.122.141`)
- **Port:** `443` (обычно)
- **Public Key:** Публичный ключ REALITY (из `/usr/local/etc/xray/config.json`)
- **Short ID:** Short ID REALITY (из конфига)
- **SNI:** SNI для REALITY (например: `google.com`)
- **API URL:** `http://your-vps-ip:8889`
- **API Token:** Токен, который вы сгенерировали для VPN API
- **Hop-сервер:** ❌ (не отмечайте)
- **Активен:** ✅ (отметьте)

#### Для hop-сервера (обход белых списков):

- **Название:** `Yandex-Hop` (или любое другое)
- **Локация:** `Германия` (или другая страна)
- **Host:** IP адрес hop-сервера (например: `51.250.43.91`)
- **Port:** `443`
- **Public Key:** Публичный ключ REALITY
- **Short ID:** Short ID REALITY
- **SNI:** SNI для REALITY (например: `eh.vk.com`)
- **API URL:** `http://your-hop-server-ip:8890`
- **API Token:** Токен, который вы сгенерировали для LTE Bypass API
- **Hop-сервер:** ✅ (отметьте!)
- **Активен:** ✅ (отметьте)

### 4. Сохраните сервер

Нажмите кнопку "Сохранить" или "Добавить сервер".

### 5. Проверка

После добавления сервер должен появиться в списке. Для hop-сервера он **не будет виден** в обычном меню VPN (это нормально, он используется только для услуги "Обход белых списков").

---

## ✅ Проверка работы

### 1. Проверка бота

1. Откройте Telegram и найдите вашего бота
2. Отправьте `/start`
3. Проверьте, что бот отвечает и показывает главное меню

### 2. Проверка создания конфигурации

1. Пополните баланс (минимум 10₽)
2. Перейдите в "🔗 VPN"
3. Выберите сервер
4. Проверьте, что конфигурация создаётся и UUID добавляется на сервер

### 3. Проверка API на VPN сервере

```bash
# На VPN сервере
curl -X POST http://localhost:8889/list-clients \
  -H "Authorization: Bearer your_token_here"
```

Должен вернуться список UUID клиентов.

### 4. Проверка услуги "Обход белых списков"

1. В боте нажмите "📡 Обход"
2. Проверьте, что показывается информация об услуге
3. Активируйте пробный период (если доступен)
4. Проверьте, что конфигурация создаётся на hop-сервере

### 5. Проверка API на hop-сервере

```bash
# На hop-сервере
curl -X POST http://localhost:8890/list-clients \
  -H "Authorization: Bearer your_token_here"
```

Должен вернуться список UUID клиентов.

### 6. Проверка админ-панели

1. Откройте `http://your-server-ip:5173`
2. Войдите с учётными данными
3. Проверьте, что видны пользователи, серверы, платежи

---

## 🚀 Запуск через PM2 (продакшен)

Для продакшена рекомендуется использовать PM2 для управления процессами.

### 1. Установите PM2

```bash
npm install -g pm2
```

### 2. Запустите все сервисы

```bash
# Из корня проекта
pm2 start ecosystem.config.js
```

Это запустит:
- `vpn-bot` - Telegram бот
- `admin-api` - API сервер админ-панели
- `admin` - Веб-интерфейс админ-панели

### 3. Проверьте статус

```bash
pm2 status
```

### 4. Просмотр логов

```bash
# Все логи
pm2 logs

# Логи конкретного процесса
pm2 logs vpn-bot
pm2 logs admin-api
pm2 logs admin
```

### 5. Сохранение конфигурации PM2

```bash
pm2 save
pm2 startup
```

Это создаст автозапуск PM2 при перезагрузке сервера.

### 6. Управление процессами

```bash
# Перезапуск
pm2 restart all
pm2 restart vpn-bot

# Остановка
pm2 stop all
pm2 stop vpn-bot

# Удаление
pm2 delete vpn-bot
```

---

## 🔧 Настройка Xray на VPN сервере

Если у вас ещё не настроен Xray на VPN сервере, используйте скрипт:

```bash
# На VPN сервере
bash scripts/setup_xray_server.sh
```

Или настройте вручную (см. `scripts/XRAYCHAIN_VLESS_SETUP.md`).

**Важно:** После настройки Xray запишите:
- Public Key
- Private Key
- Short ID
- SNI
- UUID (если есть)

Эти данные понадобятся при добавлении сервера в админ-панели.

---

## 📝 Частые проблемы и решения

### 1. Бот не отвечает

**Решение:**
- Проверьте `BOT_TOKEN` в `.env`
- Проверьте, что бот запущен: `pm2 status` или `ps aux | grep python`
- Проверьте логи: `pm2 logs vpn-bot`

### 2. API не отвечает на VPN сервере

**Решение:**
- Проверьте статус сервиса: `systemctl status vpn-api`
- Проверьте порт: `netstat -tlnp | grep 8889`
- Проверьте firewall: `ufw status`
- Проверьте логи: `journalctl -u vpn-api -f`

### 3. Конфигурации не создаются

**Решение:**
- Проверьте баланс пользователя (минимум 10₽)
- Проверьте настройки сервера в админ-панели (API URL, Token)
- Проверьте логи бота: `pm2 logs vpn-bot`
- Проверьте доступность API: `curl -X POST http://server-ip:8889/list-clients -H "Authorization: Bearer token"`

### 4. Админ-панель не открывается

**Решение:**
- Проверьте, что `admin-api` запущен: `pm2 status`
- Проверьте, что `admin` запущен: `lsof -i :5173`
- Проверьте порты в firewall
- Проверьте логи: `pm2 logs admin-api` и `pm2 logs admin`

### 5. Ошибка "ModuleNotFoundError"

**Решение:**
```bash
# Переустановите зависимости
python3 -m pip install -r requirements.txt --user

# Или используйте виртуальное окружение
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 6. API возвращает 401 Unauthorized

**Решение:**
- Проверьте токен в systemd сервисе: `systemctl show vpn-api | grep VPN_API_TOKEN`
- Проверьте токен в админ-панели (должен совпадать)
- Перезапустите сервис: `systemctl restart vpn-api`

---

## 📚 Дополнительная документация

- **PROJECT_DOCUMENTATION.md** - Подробная документация по проекту
- **FUNCTIONALITY.md** - Описание функционала
- **scripts/XRAYCHAIN_VLESS_SETUP.md** - Настройка Xray
- **scripts/LTE_BYPASS_API_SETUP.md** - Настройка API для LTE Bypass
- **HOW_TO_ACCESS_ADMIN.md** - Доступ к админ-панели

---

## 🎯 Итоговый чеклист

- [ ] Установлены все зависимости (Python, Node.js)
- [ ] Создан файл `.env` с токенами
- [ ] Бот запущен и отвечает на команды
- [ ] Настроен API на VPN серверах (порт 8889)
- [ ] Настроен API на hop-сервере для LTE Bypass (порт 8890)
- [ ] Запущена админ-панель (API на 8080, веб на 5173)
- [ ] Добавлены серверы через админ-панель
- [ ] Проверено создание конфигураций
- [ ] Проверена услуга "Обход белых списков"
- [ ] Настроен PM2 для автозапуска (опционально)

---

