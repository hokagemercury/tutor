

## 📁 Структура проекта

```
vpn_end/
├── main.py                    # Главный файл запуска бота
├── cache/                     # Система кэширования и работы с БД
│   ├── database.py           # Работа с SQLite базой данных
│   ├── cache.py              # Кэш в памяти для быстрого доступа
│   ├── users.py              # Работа с пользователями
│   ├── configs.py            # Работа с VPN конфигурациями
│   ├── payments.py           # Работа с платежами
│   ├── servers.py            # Работа с VPN серверами
│   └── ...
├── handlers/                  # Обработчики команд и сообщений
│   ├── start.py              # Команда /start
│   ├── buy.py                # Пополнение баланса
│   ├── profile.py            # Профиль пользователя
│   ├── vpn.py                # Меню VPN конфигураций
│   ├── vpn_servers.py        # Выбор серверов для создания конфигов
│   ├── lte_bypass.py         # Услуга "Обход белых списков" 
│   ├── configs.py            # Получение/удаление конфигов
│   ├── referral_partner.py   # Реферальная система
│   └── admin_panel/          # Админ-панель
│       ├── admin.py          # Главная админ-панель
│       ├── admin_users.py    # Управление пользователями
│       └── ...
├── services/                  # Интеграции с внешними сервисами
│   ├── cryptobot.py          # CryptoBot API (криптоплатежи)
│   └── lava.py               # Lava API (СБП платежи)
├── admin-api/                 # REST API для админ-панели
│   └── src/
│       └── index.js          # Node.js API сервер
├── admin/                     # Веб-интерфейс админ-панели
│   └── (React + TypeScript)
├── cache_server.py           # Сервер обновления кэша
├── messages.py               # Текстовые сообщения бота
├── data/                     # База данных SQLite
│   └── vpn_bot.db
├── static/                   # Статические файлы (изображения)
└── requirements.txt          # Python зависимости
```

---

## 🚀 Как запустить проект

### 1. Установка зависимостей

```bash
# Python зависимости
pip install -r requirements.txt

# Node.js зависимости для админ-панели
cd admin-api && npm install
cd ../admin && npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token

# База данных
DB_PATH=data/vpn_bot.db

# CryptoBot (опционально)
CRYPTOBOT_TOKEN=your_cryptobot_token

# Lava (опционально)
LAVA_SECRET_KEY=your_lava_secret
LAVA_SHOP_ID=your_lava_shop_id

# Админ-панель
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_password
PORT=8080
CACHE_SERVER_URL=http://localhost:8888
```

### 3. Запуск бота

```bash
# Простой запуск
python main.py

# Или через PM2 (рекомендуется)
pm2 start ecosystem.config.js
```

### 4. Запуск админ-панели

```bash
# API сервер
cd admin-api && npm start

# Веб-интерфейс (в другом терминале)
cd admin && npm run dev
```

---

## 💾 База данных

### Таблицы

#### `users` - Пользователи
- `user_id` (PRIMARY KEY) - Telegram ID пользователя
- `username` - Имя пользователя
- `balance` - Баланс в рублях
- `is_admin` - Администратор (0/1)
- `is_banned` - Заблокирован (0/1)
- `trial_used` - Использован пробный период (0/1)
- `trial_expires_at` - Дата истечения пробного периода
- `privacy_accepted` - Принята политика конфиденциальности (0/1)
- `created_at` - Дата регистрации

#### `configs` - VPN конфигурации
- `config_id` (PRIMARY KEY) - ID конфигурации
- `user_id` - ID пользователя
- `server_name` - Название сервера (с флагом страны)
- `vless_url` - VLESS ссылка для подключения
- `expires_at` - Дата истечения
- `is_active` - Активна (0/1)
- `server_id` - ID сервера из таблицы servers
- `uuid` - UUID для VLESS
- `created_at` - Дата создания

#### `lte_bypass_subscriptions` - Подписки на обход белых списков ⭐ НОВОЕ
- `subscription_id` (PRIMARY KEY) - ID подписки
- `user_id` - ID пользователя
- `vless_url` - VLESS ссылка
- `expires_at` - Дата истечения
- `is_active` - Активна (0/1)
- `last_refresh_at` - Последнее обновление конфига
- `refresh_count` - Количество обновлений
- `created_at` - Дата создания

#### `payments` - Платежи
- `payment_id` (PRIMARY KEY) - ID платежа
- `user_id` - ID пользователя
- `amount` - Сумма в рублях
- `status` - Статус (pending/completed/failed)
- `payment_method` - Метод оплаты (stars_topup/cryptobot_topup/lava_topup/...)
- `invoice_id` - ID счёта от платёжного провайдера
- `currency` - Валюта (XTR/RUB)
- `provider_url` - URL для оплаты
- `provider_payload` - JSON с данными провайдера
- `months` - Количество месяцев подписки
- `paid_at` - Дата оплаты
- `created_at` - Дата создания

#### `servers` - VPN серверы
- `server_id` (PRIMARY KEY) - ID сервера
- `name` - Название сервера
- `location` - Локация (страна)
- `host` - IP адрес или домен
- `port` - Порт (обычно 443)
- `public_key` - Публичный ключ для REALITY
- `short_id` - Short ID для REALITY
- `sni` - SNI (Server Name Indication)
- `is_active` - Активен (0/1)
- `is_hop_server` - Hop-сервер для мультихопа (0/1)
- `target_server_id` - ID целевого сервера (для hop)
- `api_url` - URL API для управления UUID
- `api_token` - Токен для API
- `created_at` - Дата добавления

#### `referral_links` - Реферальные ссылки
- `user_id` (PRIMARY KEY) - ID пользователя
- `referrer_id` - ID того, кто пригласил
- `referral_code` - Уникальный код реферальной ссылки
- `created_at` - Дата создания

#### `referral_progress` - Прогресс рефералов
- `user_id` (PRIMARY KEY) - ID пользователя
- `total_invites` - Всего приглашено
- `referral_balance` - Реферальный баланс
- `last_reward_at` - Последняя награда

#### `settings` - Настройки бота
- `setting_key` (PRIMARY KEY) - Ключ настройки
- `setting_value` - Значение настройки

---

## 🔧 Основные компоненты

### 1. Система кэширования (`cache/`)

**Зачем:** Для быстрого доступа к данным без постоянных запросов к БД.

**Как работает:**
- При запуске бота загружаются все данные в память
- При изменении данных обновляется и кэш, и БД
- Кэш обновляется через `cache_server.py` (порт 8888)

**Основные классы:**
- `Cache` - управление кэшем
- `Users` - работа с пользователями
- `Configs` - работа с конфигурациями
- `Payments` - работа с платежами
- `Servers` - работа с серверами

### 2. Обработчики (`handlers/`)

Каждый файл отвечает за свою часть функционала:

- **start.py** - Первый запуск, регистрация, политика конфиденциальности
- **buy.py** - Пополнение баланса (звёзды, CryptoBot, Lava)
- **profile.py** - Профиль пользователя, реферальная система
- **vpn.py** - Список конфигураций, удаление конфигов
- **vpn_servers.py** - Выбор сервера для создания конфига
- **lte_bypass.py** - Услуга "Обход белых списков" ⭐
- **configs.py** - Получение конфига по ID
- **referral_partner.py** - Реферальная система

### 3. Услуга "Обход белых списков" (`handlers/lte_bypass.py`) ⭐

**Что это:** Специальная услуга для обхода белых списков при использовании мобильного интернета.

**Как работает:**
1. Пользователь нажимает кнопку "📡 Обход" в главном меню
2. Если подписки нет - показывается информация и кнопка "Активировать"
3. При нажатии "Активировать" списывается 150₽ с баланса
4. Если баланса недостаточно - показывается сообщение "Пополните баланс в профиле"
5. После списания создаётся конфигурация на hop-сервере Германии (или обычном сервере)
6. Пользователь может обновить конфигурацию (удалить старый UUID, создать новый)
7. Защита от злоупотреблений:
   - Максимум 3 обновления в день
   - Кулдаун 10 минут между обновлениями

**Константы:**
```python
LTE_BYPASS_PRICE = 150  # Цена за месяц (списание с баланса)
MAX_REFRESHES_PER_DAY = 3  # Максимум обновлений в день
REFRESH_COOLDOWN_MINUTES = 10  # Кулдаун между обновлениями
```

**Таблица БД:** `lte_bypass_subscriptions`

**Оплата:**
- Списывается с баланса пользователя (150₽ за месяц)
- Если баланса нет - показывается сообщение с предложением пополнить баланс в профиле
- Не требует отдельной оплаты через платёжные системы

**API сервер:**
- На сервере обхода должен быть запущен `scripts/lte_bypass_server_api.py`
- Порт по умолчанию: 8890
- Эндпоинты: `/add-client`, `/remove-client`, `/list-clients`
- При обновлении конфига старый UUID удаляется, новый добавляется автоматически
- Всё работает асинхронно для максимальной скорости

### 4. Система платежей

**Методы оплаты:**

1. **Telegram Stars (Звёзды)**
   - Минимум: 50 звёзд
   - Курс: 1 звезда = 1.14₽
   - Обработчик: `handlers/buy.py` → `successful_payment_handler`

2. **CryptoBot (TON/USDT)**
   - Минимум: 70₽
   - Обработчик: `handlers/buy.py` → `check_crypto_topup_callback`
   - API: `services/cryptobot.py`

3. **Lava (СБП)**
   - Минимум: 70₽
   - Обработчик: `handlers/buy.py` → `check_lava_topup_callback`
   - API: `services/lava.py`

**Процесс оплаты:**
1. Пользователь выбирает способ оплаты
2. Создаётся запись в таблице `payments` со статусом `pending`
3. Создаётся счёт через API провайдера
4. Пользователь оплачивает
5. Бот проверяет статус оплаты (через callback или webhook)
6. При успешной оплате:
   - Обновляется статус на `completed`
   - Пополняется баланс (или активируется услуга)
   - Начисляется реферальная награда (20%)

### 5. Создание VPN конфигураций

**Процесс:**
1. Пользователь выбирает сервер в меню VPN
2. Проверяется баланс (минимум 10₽)
3. Проверяется лимит конфигураций (максимум 5)
4. Генерируется UUID
5. Создаётся VLESS URL с параметрами:
   - `type=tcp`
   - `security=reality`
   - `sni` - из настроек сервера
   - `pbk` - публичный ключ сервера
   - `sid` - short ID сервера
   - `fp=chrome` - fingerprint
   - `flow=xtls-rprx-vision` - flow для VLESS
6. UUID добавляется на сервер через API (если настроен)
7. Сохраняется в БД с датой истечения

**Расчёт срока действия:**
- Формула: `(баланс / количество_активных_конфигов) / 100 * 30` дней
- Минимум: 1 день
- Максимум: 3650 дней (10 лет)

### 6. Автоматические задачи

**В `main.py` запускаются фоновые задачи:**

1. **check_expired_configs()** - Проверка истёкших конфигов
   - Запускается каждый час
   - Деактивирует истёкшие конфигурации
   - Удаляет UUID с серверов

2. **deduct_daily_balance()** - Ежедневное списание баланса
   - Запускается в полночь
   - Списывает: `100₽ / 30 дней * количество_конфигов`
   - Если баланс < стоимости - удаляет все конфиги

3. **backup_database()** - Резервное копирование БД
   - Запускается раз в день
   - Создаёт backup файл

---

## 🔌 Интеграции

### 1. VPN Server API

**Эндпоинты (должны быть на каждом VPN сервере):**

- `POST /add-client` - Добавить UUID
  - Headers: `Authorization: Bearer {api_token}`
  - Body: `{"uuid": "uuid-string"}`
  
- `POST /remove-client` - Удалить UUID
  - Headers: `Authorization: Bearer {api_token}`
  - Body: `{"uuid": "uuid-string"}`

- `POST /list-clients` - Список всех UUID
  - Headers: `Authorization: Bearer {api_token}`

**Настройка в админ-панели:**
- Серверы → Редактировать → API URL (например: `http://IP:8889`)
- Серверы → Редактировать → API Token

### 2. CryptoBot API

**Файл:** `services/cryptobot.py`

**Методы:**
- `create_invoice()` - Создать счёт
- `get_invoice()` - Получить статус счёта

**Настройка:**
- Переменная окружения: `CRYPTOBOT_TOKEN`

### 3. Lava API

**Файл:** `services/lava.py`

**Методы:**
- `create_invoice()` - Создать счёт
- `get_invoice_status()` - Получить статус счёта

**Настройка:**
- Переменные окружения: `LAVA_SECRET_KEY`, `LAVA_SHOP_ID`

---

## 🎨 Админ-панель

### Структура

1. **API сервер** (`admin-api/src/index.js`)
   - REST API на Node.js
   - Аутентификация через JWT
   - Работа с БД через better-sqlite3

2. **Веб-интерфейс** (`admin/`)
   - React + TypeScript
   - Tailwind CSS
   - Компоненты:
     - UserManagement - управление пользователями
     - ServerManagement - управление серверами
     - PaymentLogs - логи платежей
     - ActivityLogs - логи активности

### Доступ

- URL: `http://your-domain:3000` (или через nginx)
- Логин: из переменной `ADMIN_USERNAME`
- Пароль: из переменной `ADMIN_PASSWORD`

---

## 🔐 Безопасность

1. **API токены** - хранятся в `.env`, не коммитятся в git
2. **Аутентификация админ-панели** - JWT токены
3. **Валидация платежей** - проверка статуса через API провайдеров
4. **Защита от злоупотреблений** - лимиты на обновление конфигов

---

## 📊 Реферальная система

**Как работает:**
1. Пользователь получает уникальную реферальную ссылку: `/start REFERRAL_CODE`
2. При переходе по ссылке новый пользователь регистрируется как реферал
3. При пополнении баланса рефералом:
   - 20% от суммы идёт на реферальный баланс пригласившего
   - Реферальный баланс можно вывести (через админ-панель)

**Таблицы:**
- `referral_links` - кто кого пригласил
- `referral_progress` - статистика и баланс

---

## 🐛 Отладка

### Логи

- Бот: вывод в консоль
- PM2: `pm2 logs`
- Админ-панель: логи в консоли Node.js

### Частые проблемы

1. **Бот не отвечает**
   - Проверить `BOT_TOKEN` в `.env`
   - Проверить, что бот запущен: `pm2 list`

2. **Платежи не проходят**
   - Проверить токены API (CryptoBot/Lava)
   - Проверить логи в `handlers/buy.py`

3. **Конфиги не создаются**
   - Проверить баланс пользователя
   - Проверить настройки серверов (API URL, Token)
   - Проверить логи в `cache/configs.py`

4. **Админ-панель не открывается**
   - Проверить, что API сервер запущен
   - Проверить порты (8080 для API, 3000 для веб)
   - Проверить nginx конфигурацию (если используется)

---

## 📝 Важные файлы для изучения

1. **main.py** - точка входа, запуск бота
2. **cache/database.py** - структура БД, миграции
3. **handlers/lte_bypass.py** - новая услуга "Обход белых списков"
4. **handlers/buy.py** - система платежей
5. **cache/configs.py** - создание конфигураций, работа с VPN API
6. **FUNCTIONALITY.md** - подробное описание функционала

---

---

**Версия документации:** 1.0  
**Дата обновления:** 2026-01-05
