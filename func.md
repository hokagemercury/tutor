
---

## 👤 Функционал для пользователей

### 🚀 Начало работы (`/start`)

**Файл:** `handlers/start.py`

**Что происходит:**
1. При первом запуске бот проверяет, принял ли пользователь политику конфиденциальности
2. Если не принял - показывает стикер и кнопки:
   - "🛡️ ПОЛИТИКА КОНФИДЕНЦИАЛЬНОСТИ" (ссылка на Telegra.ph)
   - "✅ Я прочитал и принимаю" (принимает политику)
3. После принятия политики пользователь получает **50₽** на баланс
4. Если пользователь перешёл по реферальной ссылке (`/start REFERRAL_CODE`), регистрируется реферал
5. Показывается главное меню с кнопками:
   - "👤 Мой профиль"
   - "🔐 VPN"

**Особенности:**
- Новые пользователи автоматически регистрируются в системе
- Заблокированные пользователи получают сообщение о блокировке
- Кэш пользователя обновляется при каждом запуске

---

### 👤 Профиль пользователя (`👤 Мой профиль`)

**Файл:** `handlers/profile.py`

**Отображаемая информация:**
- 💰 Баланс (в рублях)
- 🔐 Количество активных конфигураций (максимум 5)
- 👥 Количество приглашённых рефералов
- 🪙 Реферальный баланс (20% с каждого пополнения реферала)

**Кнопки:**
1. **💳 Пополнить баланс** → Переход к выбору способа оплаты
2. **🔗 Моя ссылка** → Показывает реферальную ссылку для приглашения друзей
3. **🧑‍💻 Тех. поддержка** → Ссылка на канал поддержки (@endeled_sup)
4. **📖 Инструкция** → Показывает пошаговую инструкцию по настройке VPN

**Особенности:**
- Профиль отображается с фото (`static/profile.png`)
- При ошибках редактирования сообщения бот автоматически удаляет старое и отправляет новое

---

### 💰 Пополнение баланса

**Файл:** `handlers/buy.py`

**Доступные способы оплаты:**

#### 1. ⭐️ Telegram Stars (Звёзды)
- Минимум: **50 звёзд**
- Курс: **1 звезда = 1.14₽**
- Процесс:
  1. Пользователь выбирает "Пополнить звёздами"
  2. Вводит количество звёзд (минимум 50)
  3. Бот создаёт инвойс через Telegram Stars API
  4. После оплаты баланс автоматически пополняется

#### 2. 💎 CryptoBot (TON/USDT)
- Минимум пополнения: **70₽**
- Процесс:
  1. Пользователь выбирает "CryptoBot"
  2. Вводит сумму в рублях (минимум 70₽)
  3. Бот создаёт счёт через CryptoBot API
  4. Пользователь оплачивает криптовалютой
  5. После подтверждения платежа баланс пополняется

**Расчёт подписки:**
- **100₽ = 30 дней подписки**
- Формула: `(баланс / количество_активных_конфигов) / 100 * 30` дней

**Особенности:**
- Минимальный баланс для создания конфига: **10₽**
- При балансе < 3₽ все конфигурации автоматически удаляются
- Пользователь получает уведомление при удалении конфигов из-за низкого баланса

---

### 🔐 VPN меню (`🔐 VPN`)

**Файл:** `handlers/vpn.py`

**Отображаемая информация:**
- Список всех активных конфигураций:
  - Флаг страны + **жирное** название страны
  - "╰ Создан ДД.ММ.ГГ"
  - **"Действительны до ДД.ММ.ГГГГ"** (общая дата для всех конфигов)
  - Информационное сообщение о продлении

**Кнопки для каждой конфигурации:**
- Название сервера (флаг + страна) - получить конфиг
- ❌ - удалить конфигурацию

**Дополнительные кнопки:**
- ➕ **Добавить конфигурацию** → Выбор сервера для создания нового конфига
- 📖 **Инструкция** → Показывает инструкцию по настройке

**Особенности:**
- Максимум **5 активных конфигураций** одновременно
- При удалении конфига оставшиеся конфиги продлеваются (дата истечения увеличивается)
- При добавлении нового конфига все конфиги пересчитываются (дата истечения уменьшается)
- Все активные конфиги имеют одинаковую дату истечения

---

### ➕ Добавление конфигурации

**Файл:** `handlers/vpn_servers.py`

**Процесс:**
1. Пользователь нажимает "➕ Добавить конфигурацию"
2. Проверки:
   - Баланс >= 10₽
   - Количество активных конфигов < 5
3. Показывается список доступных серверов:
   - 🇩🇪 Германия
   - 🇳🇱 Нидерланды
   - 🇷🇺 Россия
   - 🇺🇸 США
4. Пользователь выбирает сервер
5. Бот создаёт конфигурацию:
   - Генерирует UUID
   - Создаёт VLESS URL
   - Добавляет UUID на VPN сервер через API
   - Сохраняет конфиг в базу данных
   - Пересчитывает даты истечения всех конфигов
6. Показывается обновлённый список конфигураций

**Особенности:**
- Конфигурация создаётся из текущего баланса
- Баланс не списывается сразу - используется для расчёта времени действия
- При создании конфига все активные конфиги получают одинаковую дату истечения

---

### 📋 Управление конфигурациями

**Файл:** `handlers/configs.py`

#### Получение конфигурации (`get_config_*`)
- Пользователь нажимает на кнопку с названием сервера
- Бот отправляет VLESS URL для копирования
- Конфиг можно использовать в VPN клиенте (v2ray)

#### Удаление конфигурации (`delete_config_*`)
- Пользователь нажимает ❌ рядом с конфигом
- Конфиг деактивируется в базе данных
- UUID удаляется с VPN сервера через API
- Оставшиеся конфиги продлеваются (дата истечения увеличивается)
- Показывается обновлённый список конфигов

**Особенности:**
- При удалении конфига баланс не возвращается
- Время действия оставшихся конфигов увеличивается пропорционально

---

### 📖 Инструкция по настройке

**Файл:** `handlers/vpn_servers.py`

**Шаг 1:**
- Текст с ссылками на скачивание приложений:
  - [iPhone](https://apps.apple.com/app/v2raytun/id6476628951)
  - [Android](https://play.google.com/store/apps/details?id=com.v2raytun.android)
  - [Windows](https://storage.v2raytun.com/v2RayTun_Setup.exe)
- Фото: `static/ttt.PNG`
- Навигация: `< 1/2 >` и "🔙 назад в меню"

**Шаг 2:**
- Текст с инструкцией по добавлению конфига в приложение
- Видео: `static/ttt.MP4`
- Навигация: `< 2/2 >` и "🔙 назад в меню"

**Особенности:**
- Инструкция доступна из профиля и из VPN меню
- После просмотра инструкции пользователь возвращается в профиль

---

### 🔗 Реферальная система

**Файл:** `cache/referral.py`, `handlers/profile.py`

**Как работает:**
1. Каждый пользователь получает уникальную реферальную ссылку
2. Формат ссылки: `https://t.me/BOT_USERNAME?start=REFERRAL_CODE`
3. При переходе по ссылке новый пользователь регистрируется как реферал
4. При пополнении баланса рефералом:
   - Реферал получает обычный баланс
   - Пригласивший получает **20%** от суммы пополнения на реферальный баланс

**Отображение:**
- В профиле показывается количество рефералов и реферальный баланс
- Реферальный баланс можно использовать для покупки конфигов

**Особенности:**
- Реферальный код генерируется автоматически при первом запросе
- Реферальный баланс накапливается и может быть использован для покупки конфигов

---

## 👑 Админ панель (`/admin`)

**Файл:** `handlers/admin_panel/admin.py`

**Доступ:** Только для пользователей с флагом `is_admin = 1` в базе данных

**Главное меню:**
1. 📈 **Статистика** → Общая статистика бота
2. 👥 **Пользователи** → Управление пользователями
3. 💰 **Баланс пользователей** → Пополнение/обнуление баланса
4. 📢 **Рассылка** → Отправка сообщений всем пользователям
5. 📥 **Экспорт пользователей** → Экспорт данных пользователей в файл

---

### 📈 Статистика

**Файл:** `handlers/admin_panel/admin.py`

**Отображаемая информация:**
- 👥 Всего пользователей
- 💰 Общий доход (сумма всех успешных платежей)
- 🔗 Количество активных конфигураций
- 💳 Последний платёж (сумма и дата)

**Особенности:**
- Статистика берётся из кэша для быстрого отображения
- Обновляется в реальном времени

---

### 👥 Управление пользователями

**Файл:** `handlers/admin_panel/admin_users.py`

**Доступные действия:**

#### 1. Просмотр списка пользователей
- Показывается список всех пользователей с информацией:
  - Username
  - User ID
  - Дата регистрации
  - Использован ли триал
  - Статус блокировки

#### 2. ❌ Заблокировать пользователя
- Админ вводит username или user_id
- Пользователь блокируется
- Заблокированные пользователи не могут использовать бота

#### 3. ✅ Разблокировать пользователя
- Админ вводит username или user_id
- Пользователь разблокируется
- Может снова использовать бота

#### 4. 🔄 Сбросить триал
- Админ вводит username или user_id
- Триал пользователя сбрасывается
- Пользователь может снова использовать пробный период

**Особенности:**
- Поиск пользователя по username или user_id
- Все действия логируются

---

### 💰 Управление балансом

**Файл:** `handlers/admin_panel/admin_balance_edit.py`

**Доступные действия:**

#### 1. ➕ Добавить баланс
- Админ вводит username или user_id
- Вводит сумму для пополнения
- Баланс пользователя увеличивается
- **Пользователь получает уведомление** о пополнении баланса

#### 2. 🔄 Обнулить баланс
- Админ вводит username или user_id
- Баланс пользователя обнуляется
- Если были активные конфиги - они удаляются

**Особенности:**
- При пополнении баланса админом пользователь получает уведомление
- При обнулении баланса конфиги автоматически удаляются

---

### 📢 Рассылка сообщений

**Файл:** `handlers/admin_panel/admin_broadcast.py`

**Как работает:**
1. Админ нажимает "📢 Рассылка"
2. Отправляет сообщение (текст, фото, видео, документ)
3. Бот отправляет это сообщение всем пользователям
4. Сохраняется форматирование (жирный текст, курсив, ссылки и т.д.)

**Особенности:**
- Поддерживаются все типы сообщений (текст, фото, видео, документы)
- Форматирование сохраняется через `entities` и `caption_entities`
- Rate limiting: пауза 1 секунда каждые 10 сообщений
- Показывается статистика: отправлено, ошибок, всего пользователей
- Заблокированные пользователи пропускаются

---

### 📥 Экспорт пользователей

**Файл:** `handlers/admin_panel/admin_export_users.py`

**Что делает:**
1. Админ нажимает "📥 Экспорт пользователей"
2. Бот формирует текстовый файл со всеми пользователями
3. Для каждого пользователя:
   - User ID
   - Username (если есть)
   - First Name (получается из Telegram API)
4. Файл отправляется админу

**Формат файла:**
```
Дата создания: ДД.ММ.ГГГГ ЧЧ:ММ:СС
Всего пользователей: N
ID: 123456789
Username: username
Имя: First Name
------------------------------------------------------------
...
```

**Особенности:**
- Файл создаётся во временной директории
- После отправки файл удаляется
- Если не удаётся получить first_name из Telegram API - пропускается

---

## 🔧 Технические функции

### ⏰ Автоматические проверки

**Файл:** `main.py`

#### 1. Проверка истёкших конфигураций
- Запускается каждые **3600 секунд** (1 час)
- Проверяет все активные конфигурации
- Деактивирует истёкшие конфиги
- Удаляет UUID с VPN серверов через API

#### 2. Проверка низкого баланса
- Запускается вместе с проверкой конфигов
- Если баланс пользователя < 3₽:
  - Удаляет все активные конфиги
  - Удаляет UUID с VPN серверов
  - Отправляет уведомление пользователю

#### 3. Резервное копирование базы данных
- Запускается каждые **86400 секунд** (24 часа)
- Создаёт копию `vpn_bot.db` в папку `backups/`
- Хранится последние **10 бэкапов**
- Старые бэкапы автоматически удаляются

**Файл:** `scripts/backup_db.py`

---

### 💾 Система кэширования

**Файл:** `cache/cache.py`

**Что кэшируется:**
- 👥 Пользователи (`users`)
- 🔐 Конфигурации (`configs`)
- 💳 Платежи (`payments`)
- ⚙️ Настройки (`settings`)
- 🔗 Рефералы (`referrals`)
- 🖥️ Серверы (`servers`)

**Преимущества:**
- Быстрый доступ к данным без запросов к БД
- Автоматическое обновление кэша при изменениях
- Синхронизация с базой данных

**Особенности:**
- Кэш загружается при запуске бота
- Обновляется при каждом изменении данных
- Можно вручную перезагрузить кэш пользователя через `Cache.reload_user(user_id)`

---

### 🗄️ База данных

**Файл:** `cache/database.py`

**Таблицы:**

1. **users**
   - `user_id` (PRIMARY KEY)
   - `username`
   - `balance`
   - `is_admin`
   - `is_banned`
   - `trial_used`
   - `trial_expires_at`
   - `privacy_accepted`
   - `created_at`

2. **configs**
   - `config_id` (PRIMARY KEY)
   - `user_id`
   - `server_name`
   - `vless_url`
   - `expires_at`
   - `is_active`
   - `created_at`
   - `server_id`
   - `uuid`

3. **payments**
   - `payment_id` (PRIMARY KEY)
   - `user_id`
   - `amount`
   - `status` (pending/completed/failed)
   - `payment_type` (stars/crypto)
   - `created_at`

4. **referrals**
   - `referral_id` (PRIMARY KEY)
   - `user_id`
   - `referral_code`
   - `invited_by`
   - `created_at`

5. **servers**
   - `server_id` (PRIMARY KEY)
   - `location`
   - `host`
   - `port`
   - `public_key`
   - `short_id`
   - `sni`
   - `api_url`
   - `api_token`
   - `is_active`

6. **settings**
   - `setting_key` (PRIMARY KEY)
   - `setting_value`

**Особенности:**
- Используется SQLite (`aiosqlite`)
- Все операции асинхронные
- Автоматическое создание таблиц при первом запуске

---

### 🌐 VPN API интеграция

**Файл:** `cache/configs.py`

**Эндпоинты VPN сервера:**

1. **POST `/add-client`**
   - Добавляет UUID на VPN сервер
   - Headers: `Authorization: Bearer {api_token}`
   - Body: `{"uuid": "uuid-string"}`

2. **POST `/remove-client`**
   - Удаляет UUID с VPN сервера
   - Headers: `Authorization: Bearer {api_token}`
   - Body: `{"uuid": "uuid-string"}`

**Процесс:**
- При создании конфига UUID добавляется на сервер
- При удалении конфига UUID удаляется с сервера
- При деактивации истёкшего конфига UUID удаляется

**Особенности:**
- Поддержка HTTP и HTTPS
- Таймаут: 10 секунд (подключение: 3 секунды)
- Обработка ошибок авторизации и таймаутов

---

### 📊 Расчёт времени действия конфигураций

**Файл:** `cache/configs.py`

**Формула:**
```
дней_на_конфиг = (баланс / количество_активных_конфигов) / 100 * 30
```

**Примеры:**
- Баланс: 100₽, конфигов: 1 → 30 дней
- Баланс: 200₽, конфигов: 2 → 30 дней на каждый
- Баланс: 300₽, конфигов: 3 → 30 дней на каждый

**При добавлении конфига:**
- Все конфиги пересчитываются от текущей даты
- Новая дата истечения: `datetime.now() + timedelta(days=новое_количество_дней)`

**При удалении конфига:**
- Оставшиеся конфиги продлеваются от текущей даты истечения
- Если оставшихся дней меньше нового расчёта - добавляются дни
- Если оставшихся дней больше - дата не меняется

**Особенности:**
- Минимум: 1 день
- Максимум: 3650 дней (10 лет)
- Все активные конфиги имеют одинаковую дату истечения

---

## 🎨 Интерфейс

### 📱 Главное меню
- Кнопки внизу экрана:
  - "👤 Мой профиль"
  - "🔐 VPN"

### 🖼️ Статические файлы
- `static/start.png` - фото для VPN меню
- `static/profile.png` - фото для профиля
- `static/support.png` - фото для техподдержки
- `static/ttt.PNG` - фото для инструкции (шаг 1)
- `static/ttt.MP4` - видео для инструкции (шаг 2)

### 🎨 Форматирование сообщений
- Используется HTML разметка
- Поддерживается: `<b>жирный</b>`, `<i>курсив</i>`, `<code>код</code>`, `<a href="">ссылки</a>`, `<blockquote>цитаты</blockquote>`

---

## 🔐 Безопасность

### Проверки доступа
- Заблокированные пользователи не могут использовать бота
- Админ панель доступна только админам
- Проверка баланса перед созданием конфига
- Проверка лимита конфигов (максимум 5)

### Валидация данных
- Минимальный баланс для создания конфига: 10₽
- Минимальное пополнение: 70₽ (CryptoBot) или 50 звёзд (Stars)
- Проверка формата UUID перед добавлением на сервер
- Проверка статуса платежей перед пополнением баланса

---

## 📝 Логирование

**Что логируется:**
- Создание/удаление конфигов
- Пополнение баланса
- Ошибки VPN API
- Ошибки платежей
- Действия админов
- Автоматические проверки (истёкшие конфиги, низкий баланс)

**Формат логов:**
- `[CACHE]` - операции с кэшем
- `[VPN API]` - запросы к VPN серверам
- `[EXPIRED CONFIGS]` - проверка истёкших конфигов
- `[BALANCE CHECK]` - проверка баланса
- `[BROADCAST]` - рассылка сообщений
- `[ADMIN]` - действия админов

---

## 🚀 Запуск бота

**Файл:** `main.py`

**Процесс запуска:**
1. Инициализация базы данных
2. Загрузка кэша из БД
3. Регистрация всех роутеров
4. Запуск фоновых задач:
   - Проверка истёкших конфигов
   - Резервное копирование БД
5. Запуск сервера обновления кэша (опционально)
6. Запуск polling для получения обновлений от Telegram

**Требования:**
- Python 3.12+
- Переменные окружения в `.env`:
  - `BOT_TOKEN` - токен Telegram бота
  - `CRYPTOBOT_TOKEN` - токен CryptoBot (опционально)

---

## 📚 Структура проекта

```
vpn_end/
├── handlers/              # Обработчики команд и callback'ов
│   ├── start.py          # Команда /start
│   ├── profile.py        # Профиль пользователя
│   ├── buy.py            # Пополнение баланса
│   ├── vpn.py            # VPN меню
│   ├── vpn_servers.py    # Выбор сервера и инструкции
│   ├── configs.py        # Управление конфигами
│   └── admin_panel/      # Админ панель
│       ├── admin.py      # Главное меню админа
│       ├── admin_users.py
│       ├── admin_balance_edit.py
│       ├── admin_broadcast.py
│       └── admin_export_users.py
├── cache/                # Кэш и работа с БД
│   ├── cache.py          # Система кэширования
│   ├── database.py       # Работа с SQLite
│   ├── users.py          # Пользователи
│   ├── configs.py        # Конфигурации
│   ├── payments.py       # Платежи
│   ├── referral.py       # Реферальная система
│   └── servers.py        # VPN серверы
├── services/             # Внешние сервисы
│   └── cryptobot.py      # Интеграция с CryptoBot
├── scripts/               # Вспомогательные скрипты
│   └── backup_db.py       # Резервное копирование БД
├── static/                # Статические файлы (фото, видео)
├── main.py                # Точка входа
└── messages.py            # Текстовые сообщения
```

---

## 🔄 Потоки данных

### Создание конфигурации:
```
Пользователь → Выбор сервера → Проверка баланса → Генерация UUID → 
Создание VLESS URL → Добавление UUID на VPN сервер → Сохранение в БД → 
Обновление кэша → Пересчёт дат истечения → Отображение списка конфигов
```

### Пополнение баланса:
```
Пользователь → Выбор способа оплаты → Ввод суммы → Создание инвойса → 
Оплата → Проверка статуса → Подтверждение платежа → Пополнение баланса → 
Обновление кэша → Уведомление пользователя
```

### Удаление конфигурации:
```
Пользователь → Нажатие ❌ → Деактивация конфига → Удаление UUID с сервера → 
Удаление из БД → Обновление кэша → Пересчёт дат истечения → 
Отображение обновлённого списка
```
